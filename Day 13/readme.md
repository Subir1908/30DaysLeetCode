Solved the Length of Last word problem where we need to find the length of the last word of a string with two pointer solution.
The final algorithm has a space and time complexity of O(n) and O(1) respectively, it has a runtime of 0ms and memory of 8.84 MB.
