Completed the Remove duplicates from a sorted array problem.
Used an integer to keep track of the element and checked if there are duplicates and pushed the unique elements onto a new vector and then replaced the original nums with the new vector and returned it size.
The algoritm has a runtime of 0ms and memory of 22.81 MB.